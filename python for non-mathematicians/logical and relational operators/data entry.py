#data entry

chain = input("what is the name of your project?: ")
print(f"your project is called {chain}")

ver = input("which version is?: ")
print(f"your project version is {ver}")
#int
dev = int(input("how many versions you want to create?: "))
print(f"total number of versions {dev+1}")
#float
flow = float(input("how much is a half version for you?: "))
print(f"versions and half versions total {flow+1}")